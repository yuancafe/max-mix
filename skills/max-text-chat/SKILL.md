---
name: max-text-chat
description: Use when user wants MiniMax-M* text/coding chat via plan then run workflow, with message arrays and generation controls.
---

# max-text-chat

One skill, two stages:
- `plan`: assemble messages/system/model params into JSON
- `run`: execute `mmx text chat` from plan
